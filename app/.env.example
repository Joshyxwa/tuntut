# Server-side only. Never place either value in browser JavaScript.
ELEVENLABS_API_KEY=your_restricted_api_key
ELEVENLABS_AGENT_ID=agent_your_agent_id
# In ElevenLabs, require authentication, allowlist the production domain,
# give the key only Agents read/use access, and cap the workspace credits.
